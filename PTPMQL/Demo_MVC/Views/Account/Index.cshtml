@using Microsoft.AspNetCore.Identity
@using Demo_MVC.Models.ViewModels
@model IEnumerable<UserWithRoleVM>
<table class="table">
    <thead>
        <tr>
            <th>Username</th>
            <th>Roles</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var userWithRoles in Model)
        {
            <tr>
                <td>@userWithRoles.User.UserName</td>
                <td>
                    @foreach (var role in userWithRoles.Roles)
                    {
                        <span>@role,</span><br />
                    }
                </td>
                <td>
                    <a asp-controller="Account" asp-action="AssignRole" asp-route-id="@userWithRoles.User.Id">Assign Role</a>
                </td>
            </tr>
        }
    </tbody>
</table>